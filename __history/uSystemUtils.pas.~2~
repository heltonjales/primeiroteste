unit uSystemUtils;

interface
  function isNumber(key : char) : boolean;

implementation

uses
  System.SysUtils;

function isNumber(key : char) : boolean;
begin
  result := CharInSet(Key,['0'..'9']);
end;

end.
